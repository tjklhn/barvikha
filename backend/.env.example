# Local development example.
#
# Copy this file to `backend/.env.local` and edit values as needed.

# Backend port (default: 5000)
PORT=5000

# Token auth:
# - For quick local testing, disable token checks:
KL_REQUIRE_TOKEN=0
#
# - To mimic production, enable token checks and set allowed tokens:
# KL_REQUIRE_TOKEN=1
# KL_ACCESS_TOKENS=put-your-token-here

# Allow browser UI from any origin during local dev.
KL_ALLOW_ALL_ORIGINS=1

# Debug controls are OFF by default.
# To enable any debug feature, first set KL_ENABLE_DEBUG=1 and then specific flags below.
KL_ENABLE_DEBUG=0
KL_DEBUG_LOG_FILES=0
KL_DEBUG_MESSAGES=0
KL_DEBUG_PUBLISH=0
KL_DEBUG_FIELDS=0
KL_DEBUG_CATEGORIES=0
KL_MESSAGE_ACTION_ARTIFACTS=0

# Optional: increase deadlines if your proxies are slow.
# KL_MESSAGE_ROUTE_DEADLINE_MS=240000
# KL_MESSAGE_ACTION_DEADLINE_MS=240000

# Optional: Telegram duplication for new incoming messages.
# Messages are sent when `/api/messages` receives unread conversations.
# KL_TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# KL_TELEGRAM_CHAT_ID=-1001234567890
# Or multiple chats:
# KL_TELEGRAM_CHAT_IDS=-1001234567890,123456789
# Optional timeout for Telegram API calls (ms):
# KL_TELEGRAM_TIMEOUT_MS=12000
